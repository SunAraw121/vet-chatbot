{
    "name": "vet-chatbot-monorepo",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "install-all": "npm install --prefix backend && npm install --prefix frontend/vet-chatbot-ui",
        "backend": "npm start --prefix backend",
        "frontend": "npm run dev --prefix frontend/vet-chatbot-ui",
        "build": "npm run build --prefix frontend/vet-chatbot-ui && node scripts/sync-assets.js",
        "postinstall": "npm run install-all",
        "start": "npm start --prefix backend"
    },
    "engines": {
        "node": ">=16.0.0"
    }
}